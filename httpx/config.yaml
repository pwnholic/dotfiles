# httpx config file
# generated by https://github.com/projectdiscovery/goflags

# input file containing list of hosts to process
#list: 

# file containing raw request
#request: 

# input target host(s) to probe
#target: []

# mode of input file (burp)
#input-mode: 

# display response status-code
#status-code: false

# display response content-length
#content-length: false

# display response content-type
#content-type: false

# display response redirect location
#location: false

# display mmh3 hash for '/favicon.ico' file
#favicon: false

# display response body hash (supported: md5,mmh3,simhash,sha1,sha256,sha512)
#hash: 

# display jarm fingerprint hash
#jarm: false

# display response time
#response-time: false

# display response body line count
#line-count: false

# display response body word count
#word-count: false

# display page title
#title: false

# display first n characters of response body
#body-preview: 

# display server name
#web-server: false

# display technology in use based on wappalyzer dataset
#tech-detect: false

# path to a custom fingerprint file for technology detection
#custom-fingerprint-file: 

# display cpe (common platform enumeration) with product version based on awesome-search-queries
#cpe: false

# display wordpress plugins and themes
#wordpress: false

# display http request method
#method: false

# display server using websocket
#websocket: false

# display host ip
#ip: false

# display host cname
#cname: false

# get domain and subdomains from response body and header in jsonl/csv output
#efqdn: false

# display host asn information
#asn: false

# display cdn/waf in use
#cdn: true

# display probe status
#probe: false

# enable saving screenshot of the page using headless browser
#screenshot: false

# enable using local installed chrome for screenshot
#system-chrome: false

# start headless chrome with additional options
#headless-options: []

# enable excluding screenshot bytes from json output
#exclude-screenshot-bytes: false

# enable excluding headless header from json output
#exclude-headless-body: false

# disable saving full page screenshot
#no-screenshot-full-page: false

# set timeout for screenshot in seconds
#screenshot-timeout: 

# set idle time before taking screenshot in seconds
#screenshot-idle: 

# execute javascript code after navigation
#javascript-code: []

# match response with specified status code (-mc 200,302)
#match-code: 

# match response with specified content length (-ml 100,102)
#match-length: 

# match response body with specified line count (-mlc 423,532)
#match-line-count: 

# match response body with specified word count (-mwc 43,55)
#match-word-count: 

# match response with specified favicon hash (-mfc 1494302000)
#match-favicon: []

# match response with specified string (-ms admin)
#match-string: []

# match response with specified regex (-mr admin)
#match-regex: []

# match host with specified cdn provider (腾讯云, qrator, 云盾, arvancloud, cdnetworks, fastly, google, lgtelecom, cafe24, cloudfront, gcore, gocache, kinx, skbroadband, 百度云加速, 加速乐)
#match-cdn: []

# match response with specified response time in seconds (-mrt '< 1')
#match-response-time: 

# match response with dsl expression condition
#match-condition: 

# display response content with matched regex
#extract-regex: []

# display response content matched by a pre-defined regex (ipv4,mail,url)
#extract-preset: []

# filter response with specified status code (-fc 403,401)
#filter-code: 

# filter response with specified page type (e.g. -fpt login,captcha,parked)
#filter-page-type: []

# [deprecated: use -fpt] filter response with ml based error page detection
#filter-error-page: false

# filter out near-duplicate responses (only first response is retained)
#filter-duplicates: false

# filter response with specified content length (-fl 23,33)
#filter-length: 

# filter response body with specified line count (-flc 423,532)
#filter-line-count: 

# filter response body with specified word count (-fwc 423,532)
#filter-word-count: 

# filter response with specified favicon hash (-ffc 1494302000)
#filter-favicon: []

# filter response with specified string (-fs admin)
#filter-string: []

# filter response with specified regex (-fe admin)
#filter-regex: []

# filter host with specified cdn provider (腾讯云, qrator, 云盾, arvancloud, cdnetworks, fastly, google, lgtelecom, cafe24, cloudfront, gcore, gocache, kinx, skbroadband, 百度云加速, 加速乐)
#filter-cdn: []

# filter response with specified response time in seconds (-frt '> 1')
#filter-response-time: 

# filter response with dsl expression condition
#filter-condition: 

# strips all tags in response. supported formats: html,xml
#strip: html

# list of fields to output (comma separated)
#list-output-fields: false

# exclude output fields output based on a condition
#exclude-output-fields: []

# number of threads to use
#threads: 50

# maximum requests to send per second
#rate-limit: 150

# maximum number of requests to send per minute
#rate-limit-minute: 0

# probe all the ips associated with same host
#probe-all-ips: false

# ports to probe (nmap syntax: eg http:1,2-10,11,https:80)
#ports: Custom Ports

# path or list of paths to probe (comma-separated, file)
#path: 

# send http probes on the extracted tls domains (dns_name)
#tls-probe: false

# send http probes on the extracted csp domains
#csp-probe: false

# perform tls(ssl) data grabbing
#tls-grab: false

# probe and display server supporting http1.1 pipeline
#pipeline: false

# probe and display server supporting http2
#http2: false

# probe and display server supporting vhost
#vhost: false

# list json output field keys name that support dsl matcher/filter
#list-dsl-variables: false

# update httpx to latest version
#update: false

# disable automatic httpx update check
#disable-update-check: false

# file to write output results
#output: 

# filename to write output results in all formats
#output-all: false

# store http response to output directory
#store-response: false

# store http response to custom directory
#store-response-dir: 

# omit response body in output
#omit-body: false

# store output in csv format
#csv: false

# define output encoding
#csv-output-encoding: 

# store output in jsonl(ines) format
#json: false

# store output in markdown table format
#markdown: false

# include http response (headers) in json output (-json only)
#include-response-header: false

# include http request/response (headers + body) in json output (-json only)
#include-response: false

# include base64 encoded http request/response in json output (-json only)
#include-response-base64: false

# include redirect http chain in json output (-json only)
#include-chain: false

# include http redirect chain in responses (-sr only)
#store-chain: false

# include visual recon clusters (-ss and -sr only)
#store-vision-recon-cluster: false

# protocol to use (unknown, http11, http2 [experimental], http3 [experimental])
#protocol: 

# path to store filtered error pages
#filter-error-page-path: filtered_error_page.json

# store results in database
#result-db: false

# path to database config file
#result-db-config: 

# database type (mongodb, postgres, mysql)
#result-db-type: 

# database connection string
#result-db-conn: 

# database name
#result-db-name: httpx

# table/collection name
#result-db-table: results

# batch size for database inserts
#result-db-batch-size: 100

# omit raw request/response data from database
#result-db-omit-raw: false

# path to the httpx configuration file (default $home/.config/httpx/config.yaml)
#config: 

# list of custom resolver (file or comma separated)
#resolvers: []

# allowed list of ip/cidr's to process (file or comma separated)
#allow: Custom Global List

# denied list of ip/cidr's to process (file or comma separated)
#deny: Custom Global List

# custom tls sni name
#sni-name: 

# enable random user-agent to use
#random-agent: true

# set the referer header to the current url
#auto-referer: false

# custom http headers to send with request
#header: Custom Global Headers

# proxy (http|socks) to use (eg http://127.0.0.1:8080); falls back to http_proxy/https_proxy/no_proxy env vars
#proxy: 

# send raw requests skipping golang normalization
#unsafe: false

# resume scan using resume.cfg
#resume: false

# follow http redirects
#follow-redirects: false

# max number of redirects to follow per host
#max-redirects: 10

# follow redirects on the same host
#follow-host-redirects: false

# respect hsts response headers for redirect requests
#respect-hsts: false

# get a list of vhosts as input
#vhost-input: false

# request methods to probe, use 'all' to probe all http methods
#x: 

# post body to include in http request
#body: 

# stream mode - start elaborating input targets without sorting
#stream: false

# disable dedupe input items (only used with stream mode)
#skip-dedupe: false

# leave default http/https ports in host header (eg. http://host:80 - https://host:443
#leave-default-ports: false

# use ztls library with autofallback to standard one for tls13
#ztls: false

# avoid decoding body
#no-decode: false

# enable experimental client hello (ja3) tls randomization
#tls-impersonate: false

# disable stdin processing
#no-stdin: false

# experimental http api endpoint
#http-api-endpoint: 

# path to the secret file for authentication
#secret-file: 

# run diagnostic check up
#hc: false

# display request/response content in cli
#debug: false

# display request content in cli
#debug-req: false

# display response content in cli
#debug-resp: false

# display httpx version
#version: false

# display scan statistic
#stats: false

# optional httpx memory profile dump file
#profile-mem: 

# silent mode
#silent: false

# verbose mode
#verbose: false

# number of seconds to wait between showing a statistics update (default: 5)
#stats-interval: 0

# disable colors in cli output
#no-color: false

# trace
#trace: false

# display both probed protocol (https and http)
#no-fallback: false

# probe with protocol scheme specified in input 
#no-fallback-scheme: false

# max error count per host before skipping remaining path/s
#max-host-error: 30

# exclude host matching specified filter ('cdn', 'private-ips', cidr, ip, regex)
#exclude: []

# number of retries
#retries: 0

# timeout in seconds
#timeout: 10

# duration between each http request (eg: 200ms, 1s)
#delay: 

# max response size to save in bytes
#response-size-to-save: 50000000

# max response size to read in bytes
#response-size-to-read: 50000000

# configure projectdiscovery cloud (pdcp) api key
#auth: true

# configure projectdiscovery cloud (pdcp) api key credential file
#auth-config: 

# upload / view output in projectdiscovery cloud (pdcp) ui dashboard
#dashboard: false

# upload asset results to given team id (optional)
#team-id: 

# upload new assets to existing asset id (optional)
#asset-id: 

# assets group name to set (optional)
#asset-name: 

# upload httpx output file (jsonl) in projectdiscovery cloud (pdcp) ui dashboard
#dashboard-upload: 