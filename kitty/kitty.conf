# @noctalia/palettes/aurora.json (terminal colors)
include themes/noctalia.conf

# term variant: uniform cell width, minimal ligatures
font_family        Iosevka Term Slab
bold_font          auto
italic_font        auto
bold_italic_font   auto
# comfortable on 1920x1200 16:10
font_size 12.5
# no ligatures under the cursor while editing
disable_ligatures cursor
# default Linux compositing, crisp on dark themes
text_composition_strategy platform
# sharpest at this DPI
undercurl_style thin-sparse
# scaled by monitor DPI
box_drawing_scale 0.001, 1, 1.5, 2
# Nerd Font v3.4.0 FAQ line: single-cell symbol glyphs
symbol_map U+e000-U+e00a,U+e0a0-U+e0a2,U+e0a3,U+e0b0-U+e0b3,U+e0b4-U+e0c8,U+e0ca,U+e0cc-U+e0d7,U+e200-U+e2a9,U+e300-U+e3e3,U+e5fa-U+e6b7,U+e700-U+e8ef,U+ea60-U+ec1e,U+ed00-U+efce,U+f000-U+f2ff,U+f300-U+f381,U+f400-U+f533,U+f0001-U+f1af0 Symbols Nerd Font Mono
# tuned glyph metrics for a denser line
modify_font underline_position 2
modify_font underline_thickness 100%
modify_font strikethrough_thickness 100%
modify_font strikethrough_position 2px
modify_font cell_width 103%
modify_font cell_height 2px

# block cursor
cursor_shape block
# hollow when the OS window is unfocused
cursor_shape_unfocused hollow
# trail animation after 3ms idle
cursor_trail 3
cursor_trail_decay 0.1 0.4

# on-demand RAM, 16GB is plenty
scrollback_lines 10000
scrollback_pager_history_size 25
scrollback_fill_enlarged_window no
# sub-line touchpad scrolling
pixel_scroll yes

# hide cursor after 3s idle
mouse_hide_wait 3.0
url_style curly
detect_urls yes
strip_trailing_spaces smart
click_interval -1.0

# ~100 FPS
repaint_delay 10
input_delay 3
# anti-tearing on Intel iGPU
sync_to_monitor yes

# silent bell
enable_audio_bell no
visual_bell_duration 0.0
window_alert_on_bell yes
bell_on_tab "🔔 "

remember_window_size yes
initial_window_width 120c
initial_window_height 36c
window_padding_width 1
placement_strategy center
enabled_layouts *
window_border_width 0.5pt
draw_minimal_borders yes
confirm_os_window_close -1
# no kitty CSD titlebar; let Hyprland own borders
hide_window_decorations yes

tab_bar_edge bottom
tab_bar_style fade
tab_bar_min_tabs 2
tab_switch_strategy previous
tab_title_template "{fmt.fg.red}{bell_symbol}{activity_symbol}{fmt.fg.tab}{title}"

# kitty-level alpha (rules.lua overrides window opacity to 1.0)
background_opacity 0.6
# frosted blur via Wayland compositor; matches Hyprland blur.size=12
background_blur 12
dynamic_background_opacity no
dim_opacity 0.4

shell fish
editor .
close_on_child_death no
allow_remote_control no
update_check_interval 24
auto_reload_config 0.1
allow_hyperlinks yes
shell_integration enabled
term xterm-kitty

# auto-select Wayland backend
linux_display_server auto
# no CJK input; avoids Wayland IME latency
wayland_enable_ime no

# modifier for all built-in shortcuts
kitty_mod ctrl+shift

# temporary zoom (does not clash with ctrl+shift+z)
map ctrl+alt+z toggle_layout stack
# clear + keep screen in scrollback
map ctrl+l clear_terminal to_cursor_scroll active

