[opener]

play = [
    {
        run = 'mpv --terminal=no --force-window=yes %s1',
        desc = "Play with mpv",
        orphan = true,
        for = "unix"
    },
]

[open]

prepend_rules = [
    { mime = "video/*", use = "play" },
    { mime = "audio/*", use = "play" },
]
